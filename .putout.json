{
    "match": {
        "test": {
            "remove-unused-variables": ["on", {
                "exclude": [
                    "const should = require('should')"
                ]
            }]
        }
    }
}
